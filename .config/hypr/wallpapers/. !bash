#!/bin/bash
# number of files in wallpaper directory
$n = ls -l | wc -l

if ! test -f ./current_wallpaper.png; then
    ln -sf  1.png current_wallpaper.png


$x = ${readlink current_wallpaper.png:0:1}
ln -sf current_wallpaper.png 
#!/usr/bin/bash

swww query || swww init
swww img "$1" --transition-fps 60 --transition-type grow --transition-pos 0.925,0.977 --transition-duration 2
